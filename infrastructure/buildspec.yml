version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
  pre_build:
    commands:
      - echo "Checking the environment..."
  build:
    commands:
      - echo "Hello World! The TanStack Starter pipeline is working."
      - echo "Build started on `date`"
  post_build:
    commands:
      - echo "Build completed successfully!"
      
artifacts:
  files:
    - '**/*'