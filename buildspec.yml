version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
  pre_build:
    commands:
      - echo "Preparing environment..."
  build:
    commands:
      - echo "Hello World! This is the TanStack Starter build."
      - echo "Current date is `date`"
  post_build:
    commands:
      - echo "Build completed successfully!"

artifacts:
  files:
    - '**/*'