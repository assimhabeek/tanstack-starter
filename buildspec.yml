version: 0.2

batch:
  fast-fail: false
  build-list:

    - identifier: test
      # Must match the sub-project name exactly
      project-name: tanstack-starter/test

    - identifier: lint
      project-name: tanstack-starter/lint

    - identifier: build
      project-name: tanstack-starter/build